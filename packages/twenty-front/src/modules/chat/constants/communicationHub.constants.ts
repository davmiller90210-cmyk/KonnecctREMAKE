/** Quick reactions (Stream `reaction.type` — emoji work as custom types when enabled on the Stream app). */
export const HUB_QUICK_REACTION_TYPES = ['👍', '❤️', '😂', '😮', '🎉'] as const;

export type HubKonnectChannelKind = 'dm' | 'channel';
